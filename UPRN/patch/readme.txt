Gets *all* the latest m code from github and copies it to the routine directory
Before copying the code to the rtn directory, it stops and then starts the m web server after copying the m software
If you edit the script, please make sure you commit with UNIX line endings.
To run the patch script:
chmod +x patch.sh
./patch.sh